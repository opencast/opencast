module('Pop-up Help Test', {
  setup: function() {
  },
  teardown: function(){ }
});

test('open pop-up help message', function() {
  //Upload.showHelpBox('test');       // Yields Exception in jquery-1.3.2.js
});
