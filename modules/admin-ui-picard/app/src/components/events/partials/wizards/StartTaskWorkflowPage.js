import React from "react";
import {useTranslation} from "react-i18next";

const StartTaskWorkflowPage = ({ formik, previousPage, nextPage }) => {
    const { t } = useTranslation();

    return (
        <>
        </>
    );
};

export default StartTaskWorkflowPage;
