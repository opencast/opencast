import React from "react";
import {useTranslation} from "react-i18next";

const BumperPage = ({ formik, nextPage, previousPage }) => {
    const { t } = useTranslation();

    return (
        <div>

        </div>
    );
};

export default BumperPage;
