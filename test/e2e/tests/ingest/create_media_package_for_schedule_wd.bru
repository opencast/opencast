meta {
  name: create_media_package_for_schedule_wd
  type: http
  seq: 26
}

get {
  url: {{baseUrl}}/ingest/createMediaPackage
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

tests {
  test("Create MediaPackage for scheduling with workflow ID should succeed", function() {
    const body = res.getBody();
    expect(body).to.be.a('string');
  });
}

script:post-response {
  const body = res.getBody();
  bru.setVar("mediaPackage_E", body);
}
