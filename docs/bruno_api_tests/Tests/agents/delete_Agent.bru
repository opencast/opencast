meta {
  name: delete_Agent
  type: http
  seq: 11
}

delete {
  url: {{baseUrl}}/capture-admin/agents/test-agent-bruno
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

tests {
  test("Delete capture agent should succeed", function() {
    const body = res.getBody();
    expect(body).to.be.a('string');
    expect(body).to.include("test-agent-bruno removed");
  });
}
