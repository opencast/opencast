meta {
  name: get_AgentConfiguration_XML
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/capture-admin/agents/test-agent-bruno/configuration.xml
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

tests {
  test("Get capture agent configuration XML should succeed", function() {
    const body = res.getBody();
    expect(body).to.be.a('string');
    expect(body).to.match(/<(ns2:)?properties-response/);
    expect(body).to.include("capture.device.names");
  });
}
