meta {
  name: delete_Agent_invalid
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/capture-admin/agents/non-existent-agent-xyz
  body: none
  auth: inherit
}

assert {
  res.status: eq 404
}

tests {
  test("Delete non-existent capture agent should return 404", function() {
    expect(res.status).to.equal(404);
  });
}
